// Frontend Configuration

const config = {
    // Base URL for the backend API
    // Make sure the backend server is running and accessible at this address
    API_BASE_URL: "https://hamadnew.onrender.com/api" // Corrected: Point to Render backend
};

